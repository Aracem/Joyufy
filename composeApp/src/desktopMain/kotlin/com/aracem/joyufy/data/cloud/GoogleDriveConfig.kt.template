package com.aracem.joyufy.data.cloud

// Copy this file to GoogleDriveConfig.kt and fill in real credentials.
// GoogleDriveConfig.kt is git-ignored — do NOT commit real values.
//
// 1. Go to https://console.cloud.google.com
// 2. APIs & Services → Library → enable "Google Drive API"
// 3. APIs & Services → OAuth consent screen → External, add your email as test user
// 4. APIs & Services → Credentials → Create credentials → OAuth client ID → Desktop app
// 5. Paste client_id and client_secret below
object GoogleDriveConfig {
    const val CLIENT_ID = "YOUR_CLIENT_ID.apps.googleusercontent.com"
    const val CLIENT_SECRET = "YOUR_CLIENT_SECRET"
}
